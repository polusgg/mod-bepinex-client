namespace PolusMod {
    public enum PolusRpcCalls : byte {
        // 
        SnapTo = RpcCalls.SnapTo,
        CloseHud = 0x89,
        SetCode = 0x8b,
        ChatVisibility = 0x8d,
        Use = 0x8e
    }
}